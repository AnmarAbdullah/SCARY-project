using UnityEngine;

namespace ScaryGame.Items
{
    /// <summary>
    /// Tag the Core prefab with this so noise-emission and extraction logic
    /// can identify cores without using string tags.
    /// </summary>
    public class CoreItemMarker : MonoBehaviour { }
}
